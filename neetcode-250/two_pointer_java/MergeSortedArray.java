/*
Example 1:
Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
Output: [1,2,2,3,5,6]
Explanation: The arrays we are merging are [1,2,3] and [2,5,6].
The result of the merge is [1,2,2,3,5,6] with the underlined elements coming from nums1.
 */

public class MergeSortedArray {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int i = m - 1;
        int j = n - 1;
        int k = m + n - 1;

        while (i >= 0 && j >= 0) {
            if (nums1[i] > nums2[j]) {
                nums1[k--] = nums1[i--];
            } else {
                nums1[k--] = nums2[j--];
            }
        }

        while (j >= 0) {
            nums1[k--] = nums2[j--];
        }
    }
}

/*
     Data Structure : Array
     Approach :
        1. Initialize two pointers i and j to last index of nums1 and nums2 respectively.
        2. Initialize a variable to max index of merged array
        3. Iterate through the arrays from the end and compare the elements of both arrays.
        4. Assign the greater number to the last index of the merged array.
        5. Copy the remaining elements of nums2 to nums1.

 */
